# Руководство по вкладу в проект AI Freelance Automation

## Добро пожаловать!

Спасибо за интерес к проекту AI Freelance Automation! Это полностью автоматизированная платформа для фрилансеров, которая использует искусственный интеллект для поиска заказов, общения с клиентами и выполнения задач (транскрибация, перевод, копирайтинг). Мы рады всем, кто хочет помочь в развитии этого открытого проекта.

## 🗺️ Как можно помочь?

### 1. Сообщение об ошибках (Issues)
Если вы нашли баг или проблему в работе приложения:
1.  **Проверьте**, не создан ли уже подобный отчет в разделе [Issues](https://github.com/A-T-0-M-1-K/ai-freelance-automation/issues).
2.  Используйте шаблон "Bug Report".
3.  Включите максимально подробную информацию:
    *   **Заголовок:** Краткое описание проблемы (например, "Ошибка при загрузке модели Whisper в headless-режиме").
    *   **Шаги для воспроизведения:** Пошаговое описание, как вызвать ошибку.
    *   **Ожидаемое поведение:** Что должно было произойти.
    *   **Фактическое поведение:** Что произошло на самом деле (текст ошибки, скриншот).
    *   **Контекст:** Версия ОС, Python, установленные пакеты, конфигурация приложения (`config/automation.json`).

### 2. Предложение новых функций (Feature Requests)
У вас есть идея, как улучшить автоматизацию фриланса?
1.  Изучите [Roadmap](ROADMAP.md) и существующие Issues, чтобы убедиться, что идея еще не обсуждалась.
2.  Используйте шаблон "Feature Request".
3.  Опишите новую функцию максимально подробно: какую проблему фрилансера она решает, как должна работать, какой AI-моделью может пользоваться.
4.  Укажите, готовы ли вы принять участие в разработке.

### 3. Разработка и Pull Requests (PR)
Мы особенно ценим помощь в написании кода. Перед тем как начать работу над крупным изменением, **обязательно обсудите его в соответствующем Issue**.

#### Процесс внесения изменений:
1.  **Форкните** репозиторий и создайте новую ветку от `main`:
    ```bash
    git checkout -b feature/ваша-фича
    # или
    git checkout -b fix/исправление-бага
    ```
2.  **Установите** проект в режиме разработки:
    ```bash
    # Установите зависимости для разработки (включая линтеры и тесты)
    pip install -e ".[dev]"
    # Запустите предварительно настроенные хуки для проверки кода
    pre-commit install
    ```
3.  **Внесите изменения** в код. Следуйте стандартам проекта (см. ниже).
4.  **Протестируйте** изменения:
    ```bash
    # Запустите тесты
    pytest -v
    # Проверьте типы
    mypy ai_core/ app/
    # Запустите линтер (ruff заменяет flake8 и pylint)
    ruff check .
    # Отформатируйте код автоматически
    ruff format .
    ```
5.  **Обновите документацию**, если ваши изменения затрагивают API, конфигурацию или пользовательский интерфейс.
6.  **Зафиксируйте изменения**, используя [Conventional Commits](https://www.conventionalcommits.org/):
    ```bash
    git commit -m "feat(scraper): добавить поддержку парсинга новой платформы Kwork"
    git commit -m "fix(ai_core): исправить утечку памяти в DecisionEngine"
    git commit -m "docs(quick_start): обновить инструкцию для установки на Windows"
    ```
7.  **Отправьте** ветку в ваш форк и создайте Pull Request в основной репозиторий.
8.  **Опишите** в PR, какие изменения были сделаны и как их протестировать. Свяжите PR с соответствующим Issue (например, `Closes #15`).

## 🛠️ Стандарты разработки

### Архитектура и структура кода
*   **`ai_core/`**: "Мозг" приложения. Содержит чистую бизнес-логику автоматизации (парсинг заказов, AI-движок, принятие решений). **Не должен** зависеть от модулей `app/` (UI, конфигурация). Используйте абстракции и внедрение зависимостей.
*   **`app/`**: "Интерфейс и инфраструктура". Содержит конфигурацию, UI (PyQt5), утилиты, работу с БД. Может использовать `ai_core`.
*   **Асинхронность**: Все операции, связанные с сетью (парсинг, API-вызовы к OpenAI), файлами и долгими вычислениями AI, должны быть асинхронными (`asyncio`, `aiohttp`).
*   **Конфигурация**: Все настройки платформ и AI должны выноситься в `config/automation.json` и `.env` файлы. Не должно быть "захардкоженных" значений в коде.

### Стиль кода на Python
*   **Форматирование:** Мы используем `ruff format` (аналог Black) с длиной строки 100 символов.
*   **Сортировка импортов:** `ruff` сортирует их автоматически. Группы: стандартная библиотека, сторонние пакеты, локальные модули.
*   **Типизация:** Все публичные функции, методы классов и их аргументы должны иметь аннотации типов (type hints). Используйте `mypy` для проверки.
*   **Документация:** Обязательно добавляйте docstrings (в формате Google или NumPy) для всех модулей, классов и публичных функций. Особенно это важно для сложной AI-логики в `ai_core/`.
    ```python
    class DecisionEngine:
        """
        Ядро принятия решений о ставках на основе AI.

        Анализирует заказ, клиента и рыночную ситуацию, чтобы рассчитать
        оптимальную цену и вероятность успеха.
        """

        def calculate_bid(self, job: Job, market_context: Dict) -> float:
            """
            Рассчитывает рекомендуемую ставку для заказа.

            Args:
                job: Объект заказа для анализа.
                market_context: Текущие рыночные данные (средние ставки, конкуренция).

            Returns:
                Рекомендуемую сумму ставки в валюте заказа.

            Raises:
                AnalysisError: Если не удается проанализировать параметры заказа.
            """
            # ... код ...
    ```

### Работа с AI-моделями
*   **Локальные модели** (Whisper, переводческие): должны загружаться в папку `ai/models/`. Код должен корректно обрабатывать отсутствие модели и предлагать варианты загрузки.
*   **Внешние API** (OpenAI, DeepL): Все ключи должны браться из переменных окружения (`.env`). Код должен обрабатывать квоты, лимиты и сетевые ошибки.
*   **Кэширование:** Результаты транскрибации, переводов и анализ заказов должны кэшироваться в `ai/cache/`, чтобы избежать повторных запросов и вычислений.

### Тестирование
*   **Unit-тесты:** Для логических модулей, особенно `ai_core` (парсеры, движок решений, обработчики задач).
*   **Интеграционные тесты:** Для проверки взаимодействия модулей (например, полный цикл "нашел заказ → проанализировал → отправил ставку").
*   **Моки (Mocking):** Все внешние вызовы (API фриланс-сайтов, OpenAI, база данных) должны быть замоканы в тестах.
*   **Тесты для AI:** Фокус на тестирование стабильности, обработку ошибок и форматов ввода/вывода, а не на "качество" ответов AI.

## 🐳 Деплой и инфраструктура
*   **Docker:** Основной `Dockerfile` должен собирать минимальный образ для headless-работы (без GUI). Образ с GUI (`PyQt5`) — опционально.
*   **Конфигурация:** При запуске в Docker конфигурация должна подаваться через volume (`-v ./config:/app/config`) или переменные окружения.

## 🔒 Безопасность
*   **Секреты:** Никогда не коммитьте API-ключи, пароли от аккаунтов фриланс-платформ. Используйте `.env` файл, который добавлен в `.gitignore`.
*   **Данные пользователей:** Шифруйте чувствительные данные (историю переписок, финансовую информацию) при сохранении на диск, используя `app/utils/crypto.py`.
*   **Безопасный парсинг:** При разработке скраперов соблюдайте `robots.txt`, используйте задержки между запросами, чтобы не нагружать сервера платформ.

## ❓ Вопросы и помощь
*   **Обсуждение идей:** Используйте раздел [Discussions](https://github.com/A-T-0-M-1-K/ai-freelance-automation/discussions) для вопросов, идей и общих дискуссий.
*   **Поиск задачи:** Посмотрите Issues с метками `good first issue` или `help wanted`.
*   **Не уверены?** Если сомневаетесь в подходе — откройте Draft Pull Request для ранней обратной связи!

## Благодарности
Спасибо, что помогаете создавать будущее автоматизированного фриланса! Ваш вклад, будь то строка кода, отчет об ошибке или идея, делает этот проект лучше для всех фрилансеров.

---
*Это руководство — живой документ. Если вы видите возможность его улучшить, смело создавайте PR!*