# Продакшн-зависимости (без девелоперских инструментов)
# Использование: pip install -r requirements-prod.txt

-r requirements-base.txt

# Продакшн-сервер
gunicorn>=21.2.0,<22.0.0
uvloop>=0.17.0,<0.18.0
httptools>=0.5.0,<0.6.0

# Мониторинг
prometheus-client>=0.17.0,<0.18.0
sentry-sdk[fastapi]>=1.30.0,<2.0.0

# Кэширование
aiocache>=0.12.0,<0.13.0

# Очереди задач
celery[redis]>=5.3.0,<6.0.0